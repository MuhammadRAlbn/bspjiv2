import './bootstrap';
// Alpine.js sudah ter-bundle dengan Livewire 4
// Tidak perlu import manual

// Custom Alpine components jika diperlukan
document.addEventListener('alpine:init', () => {
    // Custom Alpine data/components bisa ditambahkan di sini
});